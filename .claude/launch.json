{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "jwservices",
      "runtimeExecutable": "python",
      "runtimeArgs": ["-c", "import os,http.server,socketserver;port=int(os.environ.get('PORT','5500'));h=http.server.SimpleHTTPRequestHandler;s=socketserver.TCPServer(('',port),h);print('serving',port);s.serve_forever()"],
      "port": 5500,
      "autoPort": true
    }
  ]
}
